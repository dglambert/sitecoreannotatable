trigger:
- main

name: $(majorMinorVersion).$(semanticVersion) # $(rev:r) # NOTE: rev resets when the default retention period expires


pool:
  vmImage: windows-latest

variables:
- name: semanticVersion
  value: $[counter(variables['majorMinorVersion'], 0)]
- name: solution
  value: 'dglambert.SitecoreAnnotatable.sln' 
- name: buildPlatform
  value: 'Any CPU'  
- name: buildConfiguration
  value: 'Release' 
- name: nuspecPath
  value: '$(build.sourcesdirectory)\dglambert.SitecoreAnnotatable.Infrastructure\dglambert.SitecoreAnnotatable.Infrastructure.nuspec'  

steps:

- task: NuGetToolInstaller@1
  displayName: 'Acquires a specific version of NuGet from the internet or the tools cache'
  inputs:
    versionSpec: 5.10.0

- task: NuGetCommand@2
  displayName: 'Restore Solution Package'
  inputs:
    command: 'restore'
    restoreSolution: $(solution)
    feedsToUse: 'config'

- task: VSBuild@1
  displayName: 'Solution Build'
  inputs:
    solution: $(solution)
    platform: $(buildPlatform)
    configuration: $(buildConfiguration)
  
- task: VSTest@2
  displayName: 'Run Tests'
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: |
      **\dglambert.SitecoreAnnotatable.Tests.dll
      !**\*TestAdapter.dll
      !**\obj\**
    searchFolder: '$(build.sourcesdirectory)'
    codeCoverageEnabled: true

- task: NuGetCommand@2
  displayName: 'Pack Nuspec'
  inputs:
    command: 'pack'
    packagesToPack: '$(nuspecPath)'
    versioningScheme: 'byBuildNumber'
